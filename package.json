{
  "name": "echopost",
  "version": "0.1.0",
  "private": true,
  "description": "EchoPost — privacy-first multi-platform publishing on Netlify",
  "type": "module",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@netlify/database": "^1.1.0",
    "@netlify/functions": "^5.3.0",
    "drizzle-orm": "beta"
  },
  "devDependencies": {
    "drizzle-kit": "beta",
    "vitest": "^2.0.0"
  }
}
